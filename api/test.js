import { Sequelize } from "sequelize";

const sequelize = new Sequelize()

const user = sequelize.define("")
async () => {

  user.findAndCountAll({
    include: [
    ],


  })
  user.destroy({ tra })
  await user.update({}, { tran })
  user.create({})
  await user.findAll({
    where: {},
    include: [{
      through:
    }],
  })
user.destroy({})
  user.findByPk("", {
    include :[
      {
        through : {
          
        },
        
      }
    ],
  })
  user.bulkCreate([],{})

  
  user.findAndCountAll({
    
  })
}

