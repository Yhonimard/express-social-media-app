import { Box, Typography } from "@mui/material"

const CommentNotFound = () => {
  return (
    <Box p={4}>
      <Typography textAlign={`center`}>No Comment here</Typography>
    </Box>
  )
}

export default CommentNotFound
