import styled from "@emotion/styled"
import { Box } from "@mui/material"

export const MsgBubleListWrapper = styled(Box)(() => ({
  flex: '1 1 100%',
  height: '100%',
  overflowY: 'auto',
  display: 'flex',
  flexDirection: 'column-reverse',
  paddingTop: 1
}))


